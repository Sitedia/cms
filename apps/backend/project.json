{
  "name": "apps/backend",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/backend/src",
  "projectType": "application",
  "tags": ["backend"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["eslint . -c ../../eslint.config.js"],
        "cwd": "apps/backend"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["jest"],
        "cwd": "apps/backend"
      }
    },
    "test:coverage": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["jest --coverage --coverageDirectory ../../coverage/libs/common"],
        "cwd": "apps/backend"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest build",
        "cwd": "apps/backend"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest start --watch",
        "cwd": "apps/backend",
        "env": {
          "NODE_ENV": "development"
        }
      }
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node ./dist/apps/backend/src/main.js"
      },
      "dependsOn": ["build"]
    },
    "docker:build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "DOCKER_BUILDKIT=1 docker build . -f apps/backend/Dockerfile -t backend --no-cache"
      }
    },
    "docker:start": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "echo 'Delete the previous container if exists ..'",
          "docker rm -f backend",
          "echo 'Create the container ...'",
          "docker run --env-file apps/backend/.env.docker --name backend -p 8801:3000 backend"
        ],
        "parallel": false
      }
    }
  }
}
